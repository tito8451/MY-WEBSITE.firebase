import { SpinnerContainer, SpinnerOverlay } from "../../components/spinner/spinner.styles";

const Spinner = () => {
  return (
    <SpinnerOverlay>
      <SpinnerContainer />
    </SpinnerOverlay>
  );
};

export default Spinner;
